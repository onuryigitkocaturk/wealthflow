
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wealthflow
    username: onur
    password: onur
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: "wealthflow-backend-jwt-secret-key-256bit-secure"
  access-token-expiration-ms: 3600000

server:
  port: 8080
debug: true
